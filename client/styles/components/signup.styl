/*

Sign up

*/

.signup
  .welcome
    text-align center
    h1
      font-size: 36px;
      font-weight: 300;
      text-transform: uppercase;
      letter-spacing: 10px;
      margin 60px 20px 15px
    p.lead
      font-size: 12px;
      font-weight: 400;
      text-transform: uppercase;
      letter-spacing: 5px;
      opacity 0.6
    #login-buttons
      margin 2em

.section-header
  text-align center
  padding 50px 0px
